<view class="list-empty {{sizeClass}}">
  <image
    class="list-empty__image"
    src="{{image}}"
    mode="aspectFit"
    lazy-load="{{true}}"
  />
  <text wx:if="{{text}}" class="list-empty__text">{{text}}</text>
  <text wx:if="{{hint}}" class="list-empty__hint">{{hint}}</text>
</view>
